package main;

import java.util.HashMap;
import java.util.Iterator;
import java.util.LinkedList;
import java.util.PriorityQueue;
import java.util.Set;

public class PriorityRanking<K extends Comparable<K>, V extends Prioritizable> {
	/*
	private LinkedList<V> rankedWordQueue; //use a queue to 
	private HashMap<K,V> hashmap;
	private PriorityQueue<V> wordsInQueue;
	
	public PriorityRanking(HashMap<K, V> list) {
		this.hashmap = list;
	}
	
	public void rank() {
		Set<K> keys = hashmap.keySet();
		Iterator<K> itr = keys.iterator();
		while(itr.hasNext()) {
			wordsInQueue.add(hashmap.get(itr.next()));
		}
	}
	
	public LinkedList<V> presentWord() {
		return null;
	}
	
	*/

}
